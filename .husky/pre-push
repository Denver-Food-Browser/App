#  Run the following on every push

#!/bin/sh

# Lint code
echo "🔍 Linting code..."
bun run lint || exit 1

# Format code
echo "🎨 Formatting code..."
bun run format || exit 1

# Type check
echo "📝 Type checking..."
bun run typecheck || exit 1

echo "✅ Pre-push checks passed!"
